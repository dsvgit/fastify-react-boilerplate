{
  "name": "api",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "tsnd -r tsconfig-paths/register --respawn --transpile-only --exit-child src/app.ts",
    "start": "node -r tsconfig-paths/register src/app.js",
    "build": "tsc -p ."
  },
  "dependencies": {
    "@types/node": "^17.0.23",
    "fastify": "^3.28.0",
    "fastify-cors": "^6.0.3",
    "tsconfig-paths": "^3.14.1",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8"
  }
}
